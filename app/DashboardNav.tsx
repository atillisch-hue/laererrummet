"use client";

// Dashboard navigation is now handled in Enhancements.tsx.
// Keeping this component empty prevents the old header buttons from being
// injected a second time while preserving the existing layout import.
export default function DashboardNav(){
  return null;
}
