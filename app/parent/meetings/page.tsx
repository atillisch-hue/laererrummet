"use client";
import Link from "next/link";
import {useEffect,useState} from "react";
import {supabase} from "../../../lib/supabase";
type Meeting={id:number;title:string;meeting_type:string;starts_at:string;ends_at:string|null;location:string|null;agenda:string|null;minutes:string|null;status:string;student_id:number|null};type Child={id:number;name:string};
export default function ParentMeetings(){const[ready,setReady]=useState(false),[meetings,setMeetings]=useState<Meeting[]>([]),[children,setChildren]=useState<Child[]>([]),[openId,setOpenId]=useState<number|null>(null),[error,setError]=useState("");useEffect(()=>{(async()=>{const{data}=await supabase.auth.getSession();const session=data.session;if(!session){location.replace("/");return}try{const r=await fetch("/api/parent/children",{headers:{Authorization:`Bearer ${session.access_token}`}}),body=await r.json();setChildren((body.children||[]) as Child[]);const{data:parts,error:pErr}=await supabase.from("meeting_participants").select("meeting_id").eq("user_id",session.user.id).eq("access_type","guardian");if(pErr)throw pErr;const ids=Array.from(new Set((parts||[]).map((p:any)=>Number(p.meeting_id))));const rows:Meeting[]=[];for(const id of ids){const{data,error:mErr}=await supabase.rpc("get_guardian_meeting",{p_meeting_id:id});if(!mErr&&data?.[0])rows.push(data[0] as Meeting)}rows.sort((a,b)=>new Date(b.starts_at).getTime()-new Date(a.starts_at).getTime());setMeetings(rows)}catch{setError("Møderne kunne ikke hentes lige nu.")}setReady(true)})()},[]);const childName=(id:number|null)=>children.find(c=>c.id===id)?.name;if(!ready)return <main style={shell}>Henter møder og referater…</main>;return <main style={{minHeight:"100vh",background:"#f5f3ee",color:"#26342e"}}><header style={{background:"#243d33",color:"white",padding:"22px 32px"}}><div style={{maxWidth:900,margin:"auto"}}><Link href="/parent" style={{color:"white",textDecoration:"none",fontWeight:800}}>← Forældreportal</Link><p style={{fontSize:11,fontWeight:900,letterSpacing:1.4,opacity:.7,margin:"22px 0 3px"}}>DIT ARKIV</p><h1 style={{fontFamily:"Georgia,serif",fontSize:36,margin:0}}>Møder & referater</h1></div></header><section style={shell}><p style={{maxWidth:680,color:"#687068",lineHeight:1.55}}>Her finder du de møder, hvor skolen har inviteret dig som forælder eller værge. Du ser kun det officielle mødemateriale. Skolens interne arbejdsnoter er ikke en del af denne visning.</p>{error&&<div style={warning}>{error}</div>}{!error&&!meetings.length&&<div style={card}><strong>Ingen møder i arkivet endnu.</strong><p style={{marginBottom:0,color:"#687068"}}>Når du inviteres til et møde, bliver det synligt her.</p></div>}<div style={{display:"grid",gap:12,marginTop:24}}>{meetings.map(m=>{const open=openId===m.id,dt=new Date(m.starts_at);return <article key={m.id} style={card}><button onClick={()=>setOpenId(open?null:m.id)} style={{width:"100%",border:0,background:"transparent",padding:0,textAlign:"left",color:"inherit",cursor:"pointer"}}><small style={eyebrow}>{m.meeting_type.toUpperCase()}{childName(m.student_id)?` · ${childName(m.student_id)!.toUpperCase()}`:""}</small><h2 style={{fontFamily:"Georgia,serif",fontSize:25,margin:"5px 0"}}>{m.title}</h2><div style={{color:"#687068"}}>{dt.toLocaleDateString("da-DK",{weekday:"long",day:"numeric",month:"long",year:"numeric"})} · {dt.toLocaleTimeString("da-DK",{hour:"2-digit",minute:"2-digit"})}{m.location?` · ${m.location}`:""}</div><strong style={{display:"block",marginTop:10,color:"#526b60"}}>{open?"Luk referat ↑":"Åbn referat ↓"}</strong></button>{open&&<div style={{marginTop:18,paddingTop:18,borderTop:"1px solid #e3dfd7"}}><h3 style={h3}>Dagsorden</h3><div style={textBox}>{m.agenda||"Ingen dagsorden er delt endnu."}</div><h3 style={{...h3,marginTop:20}}>Officielt referat</h3><div style={textBox}>{m.minutes||"Referatet er ikke skrevet eller delt endnu."}</div><div style={{marginTop:16,padding:11,background:"#e7eee9",borderRadius:8,fontSize:13,color:"#52635a"}}>Dette er skolens officielle referat. Interne arbejdsnoter vises ikke i forældreportalen.</div></div>}</article>})}</div></section></main>}
const shell:React.CSSProperties={maxWidth:900,margin:"auto",padding:"32px 24px 70px"};const card:React.CSSProperties={background:"white",border:"1px solid #ddd9d0",borderRadius:13,padding:20};const warning:React.CSSProperties={marginTop:20,padding:16,background:"#fff3cd",borderRadius:10};const eyebrow:React.CSSProperties={fontSize:11,fontWeight:900,letterSpacing:1.2,color:"#718077"};const h3:React.CSSProperties={fontFamily:"Georgia,serif",fontSize:21,margin:"0 0 8px"};const textBox:React.CSSProperties={whiteSpace:"pre-wrap",lineHeight:1.6,color:"#46534c",background:"#faf9f6",padding:14,borderRadius:9};