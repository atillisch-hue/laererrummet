"use client";
import {useEffect,useState} from "react";
import {supabase} from "../../lib/supabase";
import {hasRole} from "../../lib/roles";

type Child={id:number;name:string;class_id:number;class_name:string|null};
export default function ParentPage(){
 const[ready,setReady]=useState(false),[email,setEmail]=useState(""),[children,setChildren]=useState<Child[]>([]),[activeId,setActiveId]=useState<number|null>(null),[dbReady,setDbReady]=useState(true),[canAdmin,setCanAdmin]=useState(false),[canTeacher,setCanTeacher]=useState(false);
 useEffect(()=>{(async()=>{const{data}=await supabase.auth.getSession();const user=data.session?.user;if(!user){window.location.replace("/");return}if(!hasRole(user,"parent")){window.location.replace("/");return}setEmail(user.email||"");setCanAdmin(hasRole(user,"admin"));setCanTeacher(hasRole(user,"teacher")||hasRole(user,"admin"));const{data:portal,error}=await supabase.rpc("parent_portal_data");if(error)setDbReady(false);else{const list=(portal?.children||[]) as Child[];setChildren(list);setActiveId(list[0]?.id||null)}setReady(true)})()},[]);
 async function logout(){await supabase.auth.signOut();window.location.href="/"}
 if(!ready)return <main className="login"><div className="loginCard"><h1>Forældreportalen</h1><p>Henter dit område…</p></div></main>;
 const active=children.find(c=>c.id===activeId)||children[0];
 const cards=[{icon:"▦",title:"Skema",text:"Se dit barns skema og dagens undervisning."},{icon:"✓",title:"Fravær",text:"Få overblik over registreret fravær."},{icon:"✎",title:"Opgaver",text:"Se aktuelle opgaver og afleveringer."},{icon:"✦",title:"Beskeder",text:"Information fra skolen og barnets lærere."}];
 return <main style={{minHeight:"100vh",background:"#f5f3ee",color:"#26342e"}}><header style={{background:"#243d33",color:"white",padding:"22px 32px"}}><div style={{maxWidth:1100,margin:"0 auto",display:"flex",justifyContent:"space-between",alignItems:"center",gap:18,flexWrap:"wrap"}}><div><strong style={{fontFamily:"Georgia,serif",fontSize:25}}>Klasseværelset · Forældre</strong><small style={{display:"block",opacity:.75,marginTop:3}}>{email}</small></div><div style={{display:"flex",gap:9,flexWrap:"wrap"}}>{canTeacher&&<button onClick={()=>window.location.href="/noticeboard"} style={{padding:"10px 14px",borderRadius:8,border:"1px solid white",background:"white",color:"#365044",fontWeight:800,cursor:"pointer"}}>✎ Lærerområdet</button>}{canAdmin&&<button onClick={()=>window.location.href="/admin"} style={{padding:"10px 14px",borderRadius:8,border:"1px solid white",background:"white",color:"#365044",fontWeight:800,cursor:"pointer"}}>⚙ Administration</button>}<button onClick={logout} style={{padding:"10px 16px",borderRadius:8,border:"1px solid rgba(255,255,255,.5)",background:"transparent",color:"white",cursor:"pointer"}}>Log ud</button></div></div></header><section style={{maxWidth:1100,margin:"0 auto",padding:"52px 24px"}}><p className="eyebrow">FORÆLDREPORTAL</p><h1 style={{fontFamily:"Georgia,serif",fontSize:42,margin:"8px 0"}}>Velkommen</h1><p style={{maxWidth:720,fontSize:18,color:"#6d746f",lineHeight:1.55}}>Her får du ét roligt overblik over dit barns skolehverdag. Du ser kun oplysninger om de børn, din konto er knyttet til.</p>{!dbReady&&<div style={{marginTop:24,padding:18,background:"#fff3cd",border:"1px solid #ead28b",borderRadius:10}}><strong>Forældrekoblingen kunne ikke hentes.</strong></div>}{dbReady&&children.length===0&&<div style={{marginTop:24,padding:18,background:"white",border:"1px solid #ddd9d0",borderRadius:10}}><strong>Din konto er klar.</strong><div style={{marginTop:5,color:"#6d746f"}}>Skolen mangler bare at knytte dit barn til din forældrekonto.</div></div>}{children.length>0&&<><div style={{display:"flex",gap:10,flexWrap:"wrap",marginTop:28}}>{children.map(c=><button key={c.id} onClick={()=>setActiveId(c.id)} style={{padding:"11px 16px",borderRadius:9,border:active?.id===c.id?"1px solid #365044":"1px solid #d8d5cd",background:active?.id===c.id?"#365044":"white",color:active?.id===c.id?"white":"#27352d",fontWeight:800,cursor:"pointer"}}>{c.name}{c.class_name?` · ${c.class_name}`:""}</button>)}</div><div style={{marginTop:25}}><p style={{fontSize:11,fontWeight:800,letterSpacing:1.5,color:"#718077",marginBottom:6}}>DU SER NU</p><h2 style={{fontFamily:"Georgia,serif",fontSize:30,margin:0}}>{active?.name}</h2>{active?.class_name&&<p style={{color:"#747a75",marginTop:5}}>{active.class_name}</p>}</div></>}<div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(230px,1fr))",gap:18,marginTop:30}}>{cards.map(c=><article key={c.title} style={{background:"white",border:"1px solid #ddd9d0",borderRadius:14,padding:24,minHeight:165,opacity:active?1:.55}}><div style={{fontSize:30}}>{c.icon}</div><h2 style={{fontFamily:"Georgia,serif",margin:"14px 0 8px"}}>{c.title}</h2><p style={{color:"#687068",lineHeight:1.5}}>{c.text}</p></article>)}</div></section></main>
}