"use client";
import {useEffect,useMemo,useState} from "react";
import {supabase} from "../lib/supabase";
type ClosedDay={date:string;label:string};
const iso=(d:Date)=>{const y=d.getFullYear(),m=String(d.getMonth()+1).padStart(2,"0"),day=String(d.getDate()).padStart(2,"0");return `${y}-${m}-${day}`};
function week(s:string){const d=new Date(s+"T12:00:00"),x=new Date(Date.UTC(d.getFullYear(),d.getMonth(),d.getDate())),day=x.getUTCDay()||7;x.setUTCDate(x.getUTCDate()+4-day);const ys=new Date(Date.UTC(x.getUTCFullYear(),0,1));return Math.ceil((((x.getTime()-ys.getTime())/86400000)+1)/7)}
export default function SchoolCalendarWidget({selectedDate,onSelectDate,markedDates=[]}:{selectedDate?:string;onSelectDate?:(date:string)=>void;markedDates?:string[]}){const[closed,setClosed]=useState<ClosedDay[]>([]),[view,setView]=useState(()=>{const n=selectedDate?new Date(selectedDate+"T12:00:00"):new Date();return new Date(n.getFullYear(),n.getMonth(),1)});useEffect(()=>{supabase.from("school_settings").select("closed_days").eq("id",1).maybeSingle().then(({data})=>{if(Array.isArray(data?.closed_days))setClosed(data.closed_days)})},[]);useEffect(()=>{if(selectedDate){const d=new Date(selectedDate+"T12:00:00");setView(new Date(d.getFullYear(),d.getMonth(),1))}},[selectedDate]);const marked=useMemo(()=>new Map(closed.map(x=>[x.date,x.label])),[closed]);const special=useMemo(()=>new Set(markedDates),[markedDates]);const weeks=useMemo(()=>{const first=new Date(view.getFullYear(),view.getMonth(),1);let d=new Date(first);d.setDate(d.getDate()-((d.getDay()+6)%7));const last=new Date(view.getFullYear(),view.getMonth()+1,0),out:{num:number;days:Date[]}[]=[];while(d<=last){const days=Array.from({length:7},(_,i)=>{const x=new Date(d);x.setDate(x.getDate()+i);return x});out.push({num:week(iso(d)),days});d.setDate(d.getDate()+7)}return out},[view]);const events=closed.filter(x=>{const d=new Date(x.date+"T12:00:00");return d.getFullYear()===view.getFullYear()&&d.getMonth()===view.getMonth()}).reduce<{label:string;dates:string[]}[]>((a,x)=>{let g=a.find(y=>y.label===x.label);if(!g){g={label:x.label,dates:[]};a.push(g)}g.dates.push(x.date);return a},[]);return <section style={{background:"white",border:"1px solid #ddd9d0",borderRadius:15,padding:20}}><div style={{display:"flex",justifyContent:"space-between",alignItems:"center",gap:10}}><div><p style={{fontSize:10,fontWeight:800,letterSpacing:1.5,color:"#718077",margin:0}}>SKOLEKALENDER</p><h2 style={{fontFamily:"Georgia,serif",textTransform:"capitalize",margin:"6px 0 0"}}>{view.toLocaleDateString("da-DK",{month:"long",year:"numeric"})}</h2></div><div style={{display:"flex",gap:6}}><button style={nav} onClick={()=>setView(v=>new Date(v.getFullYear(),v.getMonth()-1,1))}>←</button><button style={nav} onClick={()=>setView(v=>new Date(v.getFullYear(),v.getMonth()+1,1))}>→</button></div></div><div style={{display:"grid",gridTemplateColumns:"34px repeat(5,1fr)",gap:3,marginTop:14}}><b style={head}>U</b>{["M","T","O","T","F"].map((x,i)=><b key={i} style={head}>{x}</b>)}{weeks.map((w,i)=><div key={`${w.num}-${i}`} style={{display:"contents"}}><span style={wk}>{w.num}</span>{w.days.slice(0,5).map(d=>{const s=iso(d),off=marked.get(s),other=d.getMonth()!==view.getMonth(),today=s===iso(new Date()),selected=s===selectedDate,hasSpecial=special.has(s);return <button key={s} onClick={()=>onSelectDate?.(s)} title={off||"Vis dagens skema"} style={{...day,opacity:other?.3:1,background:selected?"#365044":off?"#efe0b7":today?"#e7eee9":"#faf9f6",color:selected?"white":"inherit",borderColor:selected?"#365044":today?"#86a294":"#eee9df",cursor:onSelectDate?"pointer":"default"}}><strong>{d.getDate()}</strong>{(off||hasSpecial)&&<span style={{display:"block",width:5,height:5,borderRadius:9,background:selected?"#dfa94f":hasSpecial?"#486b59":"#a57c2b",margin:"3px auto 0"}}/>}</button>})}</div>)}</div>{events.length>0&&<div style={{borderTop:"1px solid #eee9df",marginTop:14,paddingTop:10}}>{events.slice(0,3).map(e=><div key={e.label} style={{fontSize:12,color:"#59645e",marginTop:5}}><strong>{e.label}</strong> · {new Date(e.dates.sort()[0]+"T12:00:00").toLocaleDateString("da-DK",{day:"numeric",month:"short"})}</div>)}</div>}</section>}
const nav:React.CSSProperties={width:32,height:30,border:"1px solid #d8d4ca",borderRadius:7,background:"#faf8f3",cursor:"pointer"};const head:React.CSSProperties={fontSize:10,textAlign:"center",color:"#7a817b",padding:3};const wk:React.CSSProperties={display:"grid",placeItems:"center",fontSize:10,fontWeight:800,color:"#486b59",background:"#f0eadc",borderRadius:5,minHeight:32};const day:React.CSSProperties={display:"grid",placeItems:"center",minHeight:32,border:"1px solid",borderRadius:5,fontSize:11,fontFamily:"inherit"};