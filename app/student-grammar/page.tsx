"use client";

import { useEffect, useMemo, useState } from "react";
import { supabase } from "../../lib/supabase";

const verbQuestions=[
 {q:"Hvilket ord er udsagnsord i sætningen: ‘Maja løber hurtigt hjem.’?",options:["Maja","løber","hurtigt","hjem"],answer:"løber",why:"‘Løber’ fortæller, hvad Maja gør. Derfor er det et udsagnsord."},
 {q:"Hvilket ord er udsagnsord i sætningen: ‘Børnene byggede en hule.’?",options:["Børnene","byggede","en","hule"],answer:"byggede",why:"‘Byggede’ beskriver handlingen og står her i datid."},
 {q:"Hvilken sætning indeholder et udsagnsord i nutid?",options:["Hun sang højt.","Hun synger højt.","Hun har sunget højt.","Hun ville synge højt."],answer:"Hun synger højt.",why:"‘Synger’ står i nutid. ‘Sang’ er datid, mens de andre består af flere verballed."},
 {q:"Hvad sker der med betydningen, når ‘gik’ ændres til ‘listede’ i: ‘Han gik/listede ind i rummet.’?",options:["Betydningen ændres ikke.","Bevægelsen bliver mere præcis og skaber en stemning.","Sætningen bliver til et spørgsmål.","Ordet bliver et navneord."],answer:"Bevægelsen bliver mere præcis og skaber en stemning.",why:"Begge ord er udsagnsord, men ‘listede’ fortæller mere præcist hvordan personen bevæger sig og påvirker tekstens stemning."},
 {q:"Vælg det mest præcise udsagnsord: ‘Hunden ___ hen mod døren, da den hørte postbuddet.’",options:["var","bevægede sig","styrtede","gjorde"],answer:"styrtede",why:"‘Styrtede’ giver et tydeligt billede af en hurtig og voldsom bevægelse."}
];

export default function StudentGrammar(){
 const[loading,setLoading]=useState(true),[assignment,setAssignment]=useState<any>(null),[error,setError]=useState(""),[answers,setAnswers]=useState<Record<number,string>>({}),[submitted,setSubmitted]=useState(false),[saving,setSaving]=useState(false),[saveState,setSaveState]=useState("");
 useEffect(()=>{(async()=>{const code=sessionStorage.getItem("klassevaerelset-student-code");const id=new URLSearchParams(window.location.search).get("assignment");if(!code||!id){setError("Åbn opgaven fra din elevside.");setLoading(false);return}const{data,error}=await supabase.rpc("student_grammar_assignments",{p_access_code:code});if(error||!data?.ok){setError("Kunne ikke hente grammatikopgaven.");setLoading(false);return}const found=(data.assignments||[]).find((x:any)=>String(x.id)===id);if(!found){setError("Denne opgave er ikke tildelt din klasse.");setLoading(false);return}setAssignment(found);setLoading(false)})()},[]);
 const questions=useMemo(()=>assignment?.topic==="Udsagnsord"?verbQuestions:[],[assignment]);const score=questions.filter((q,i)=>answers[i]===q.answer).length;
 const submit=async()=>{if(!assignment||Object.keys(answers).length!==questions.length)return;setSubmitted(true);setSaving(true);setSaveState("Gemmer resultat…");const code=sessionStorage.getItem("klassevaerelset-student-code");const{data,error}=await supabase.rpc("save_student_grammar_attempt",{p_access_code:code,p_assignment_id:assignment.id,p_answers:answers,p_score:score,p_max_score:questions.length});if(error||!data?.ok)setSaveState("Resultatet kunne ikke gemmes");else setSaveState("Resultatet er gemt ✓");setSaving(false)};
 if(loading)return <main style={{padding:50}}>Åbner grammatikopgaven…</main>;
 return <main style={{minHeight:"100vh",background:"#f5f3ee",padding:"42px 24px 80px"}}><section style={{maxWidth:900,margin:"0 auto"}}><a href="/?student=1" style={{color:"#526b60",fontWeight:800,textDecoration:"none"}}>← Til mine opgaver</a>{error?<div style={{marginTop:30,background:"white",padding:28,borderRadius:14,border:"1px solid #ddd9d0"}}><h1>Hov</h1><p>{error}</p></div>:<><p style={{marginTop:38,fontSize:11,fontWeight:800,letterSpacing:1.7,color:"#718077"}}>GRAMMATIK · {assignment.area.toUpperCase()}</p><h1 style={{fontFamily:"Georgia,serif",fontSize:42,margin:"8px 0"}}>{assignment.title}</h1><p style={{fontSize:18,color:"#707670",lineHeight:1.55}}>Arbejd dig gennem opgaverne. Når du retter, får du ikke bare at vide om svaret er rigtigt — du får også forklaringen.</p>{questions.length===0?<div style={{marginTop:30,background:"white",padding:28,borderRadius:14,border:"1px solid #ddd9d0"}}><h2>{assignment.topic}</h2><p>Opgaver til dette emne er på vej.</p></div>:<><div style={{display:"grid",gap:16,marginTop:28}}>{questions.map((q,i)=><article key={i} style={{background:"white",padding:24,borderRadius:14,border:"1px solid #ddd9d0"}}><div style={{fontSize:11,fontWeight:800,letterSpacing:1.4,color:"#718077"}}>OPGAVE {i+1} AF {questions.length}</div><h2 style={{fontFamily:"Georgia,serif",fontSize:22,lineHeight:1.35,margin:"10px 0 16px"}}>{q.q}</h2><div style={{display:"grid",gap:8}}>{q.options.map(opt=>{const chosen=answers[i]===opt;const correct=submitted&&opt===q.answer;const wrong=submitted&&chosen&&opt!==q.answer;return <button key={opt} disabled={submitted} onClick={()=>setAnswers(a=>({...a,[i]:opt}))} style={{padding:"12px 14px",textAlign:"left",borderRadius:9,border:`2px solid ${correct?'#5f8068':wrong?'#b86b62':chosen?'#526b60':'#e1ddd5'}`,background:correct?'#edf5ef':wrong?'#fff0ed':chosen?'#edf1ec':'#fff',fontWeight:chosen||correct?800:600,cursor:submitted?'default':'pointer'}}>{opt}</button>})}</div>{submitted&&<div style={{marginTop:14,padding:"12px 14px",borderRadius:9,background:answers[i]===q.answer?'#edf5ef':'#fff7e8',lineHeight:1.5}}><strong>{answers[i]===q.answer?'Rigtigt ✓':'Ikke helt endnu'}</strong><br/>{q.why}</div>}</article>)}</div>{!submitted?<button disabled={Object.keys(answers).length!==questions.length||saving} onClick={submit} style={{marginTop:22,width:"100%",padding:"14px 18px",border:0,borderRadius:10,background:"#365044",color:"white",fontWeight:800,fontSize:16,opacity:Object.keys(answers).length!==questions.length?.45:1,cursor:Object.keys(answers).length!==questions.length?'not-allowed':'pointer'}}>Ret mine svar</button>:<div style={{marginTop:22,padding:24,borderRadius:14,background:"#273f35",color:"white",textAlign:"center"}}><div style={{fontSize:12,fontWeight:800,letterSpacing:1.5,opacity:.75}}>DIT RESULTAT</div><strong style={{display:"block",fontFamily:"Georgia,serif",fontSize:38,marginTop:6}}>{score} / {questions.length}</strong><p style={{marginBottom:10,opacity:.9}}>{score===questions.length?'Stærkt. Du har styr på udsagnsordene.':score>=3?'Godt på vej. Læs forklaringerne og se, hvor du kan blive skarpere.':'Brug forklaringerne som hjælp — det er dem, du lærer af.'}</p><small style={{opacity:.75,fontWeight:700}}>{saveState}</small></div>}</>}</>}</section></main>
}
