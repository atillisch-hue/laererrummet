"use client";

import Link from "next/link";
import { useEffect, useMemo, useState } from "react";
import { supabase } from "../../../../lib/supabase";
import { hasRole } from "../../../../lib/roles";

type Student = { id:number; name:string; class_id:number|null };
type Klass = { id:number; name:string };
type Abs = { id:number; student_id:number; absence_date:string; status:string; note:string|null };

const LABELS:Record<string,string>={sick:"Syg",unexcused:"Ulovligt fravær",excused:"Lovligt fravær",left_early:"Gået tidligt",late:"Forsent fremmøde",absent:"Fravær"};
const today=()=>new Date().toISOString().slice(0,10);
const monthsAgo=(n:number)=>{const d=new Date();d.setMonth(d.getMonth()-n);return d.toISOString().slice(0,10)};

export default function AbsenceStatistics(){
 const[ready,setReady]=useState(false),[students,setStudents]=useState<Student[]>([]),[classes,setClasses]=useState<Klass[]>([]),[rows,setRows]=useState<Abs[]>([]);
 const[classId,setClassId]=useState<number|"">(""),[from,setFrom]=useState(monthsAgo(3)),[to,setTo]=useState(today()),[studentId,setStudentId]=useState<number|"">("");
 useEffect(()=>{(async()=>{const{data}=await supabase.auth.getSession();if(!data.session?.user||!hasRole(data.session.user,"admin")){location.replace("/");return}const[{data:s},{data:c},{data:a}]=await Promise.all([supabase.from("students").select("id,name,class_id").order("name"),supabase.from("classes").select("id,name").order("name"),supabase.from("student_absence").select("id,student_id,absence_date,status,note").order("absence_date")]);setStudents((s||[]) as Student[]);setClasses((c||[]) as Klass[]);setRows((a||[]) as Abs[]);if(c?.[0])setClassId(c[0].id);setReady(true)})()},[]);
 const classStudents=useMemo(()=>students.filter(s=>s.class_id===Number(classId)),[students,classId]);
 const filtered=useMemo(()=>rows.filter(r=>{const s=students.find(x=>x.id===r.student_id);return !!s&&s.class_id===Number(classId)&&r.absence_date>=from&&r.absence_date<=to&&(!studentId||r.student_id===Number(studentId))}),[rows,students,classId,from,to,studentId]);
 const stats=useMemo(()=>classStudents.filter(s=>!studentId||s.id===Number(studentId)).map(s=>{const r=filtered.filter(x=>x.student_id===s.id);return {student:s,total:r.length,sick:r.filter(x=>x.status==="sick").length,excused:r.filter(x=>x.status==="excused").length,unexcused:r.filter(x=>x.status==="unexcused").length,late:r.filter(x=>x.status==="late").length,early:r.filter(x=>x.status==="left_early").length}}),[classStudents,filtered,studentId]);
 const totals=useMemo(()=>({all:filtered.length,sick:filtered.filter(x=>x.status==="sick").length,excused:filtered.filter(x=>x.status==="excused").length,unexcused:filtered.filter(x=>x.status==="unexcused").length,late:filtered.filter(x=>x.status==="late").length}),[filtered]);
 if(!ready)return <main style={{padding:50}}>Henter statistik…</main>;
 return <main style={{minHeight:"100vh",background:"#f5f2ea",color:"#26342e"}}><header style={{background:"#486b59",color:"white",padding:"18px 6vw"}}><div style={{maxWidth:1100,margin:"auto",display:"flex",justifyContent:"space-between",gap:15,flexWrap:"wrap"}}><strong style={{fontSize:22}}>Administration · Fraværsstatistik</strong><div><Link href="/admin/absence" style={{color:"white",marginRight:20}}>← Før fravær</Link><Link href="/admin" style={{color:"white"}}>Administration</Link></div></div></header><section style={{maxWidth:1100,margin:"auto",padding:"42px 24px"}}><p className="eyebrow">STATISTIK</p><h1 style={{fontFamily:"Georgia,serif",fontSize:40,marginBottom:8}}>Fraværsstatistik</h1><p style={{color:"#687068"}}>Statistikken beregnes direkte fra skolens fraværsregistreringer og opdateres automatisk.</p><div style={{display:"flex",gap:12,flexWrap:"wrap",margin:"24px 0"}}><select value={classId} onChange={e=>{setClassId(Number(e.target.value));setStudentId("")}} style={{padding:10,minWidth:210}}>{classes.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select><select value={studentId} onChange={e=>setStudentId(e.target.value?Number(e.target.value):"")} style={{padding:10,minWidth:210}}><option value="">Alle elever</option>{classStudents.map(s=><option key={s.id} value={s.id}>{s.name}</option>)}</select><input type="date" value={from} onChange={e=>setFrom(e.target.value)} style={{padding:10}}/><input type="date" value={to} onChange={e=>setTo(e.target.value)} style={{padding:10}}/></div><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(150px,1fr))",gap:12,marginBottom:24}}>{[["Registreringer",totals.all],["Syg",totals.sick],["Lovligt",totals.excused],["Ulovligt",totals.unexcused],["Forsent",totals.late]].map(([label,value])=><article key={String(label)} style={{background:"white",border:"1px solid #ddd9d0",borderRadius:14,padding:18}}><div style={{color:"#687068",fontSize:13,fontWeight:700}}>{label}</div><strong style={{display:"block",fontFamily:"Georgia,serif",fontSize:32,marginTop:6}}>{value}</strong></article>)}</div><section style={{background:"white",border:"1px solid #ddd9d0",borderRadius:14,padding:22,overflowX:"auto"}}><h2 style={{fontFamily:"Georgia,serif",marginTop:0}}>Elevoversigt</h2><table style={{width:"100%",borderCollapse:"collapse",minWidth:720}}><thead><tr>{["Elev","I alt","Syg","Lovligt","Ulovligt","Forsent","Gået tidligt"].map(x=><th key={x} style={{textAlign:x==="Elev"?"left":"center",padding:"10px 8px",borderBottom:"1px solid #ddd"}}>{x}</th>)}</tr></thead><tbody>{stats.map(x=><tr key={x.student.id}><td style={{padding:"12px 8px",borderBottom:"1px solid #eee",fontWeight:700}}>{x.student.name}</td>{[x.total,x.sick,x.excused,x.unexcused,x.late,x.early].map((v,i)=><td key={i} style={{padding:"12px 8px",borderBottom:"1px solid #eee",textAlign:"center"}}>{v}</td>)}</tr>)}</tbody></table>{!stats.length&&<p style={{color:"#777"}}>Ingen elever at vise.</p>}</section><section style={{background:"white",border:"1px solid #ddd9d0",borderRadius:14,padding:22,marginTop:24}}><h2 style={{fontFamily:"Georgia,serif",marginTop:0}}>Registreringer i perioden</h2>{filtered.slice().reverse().map(r=>{const s=students.find(x=>x.id===r.student_id);return <div key={r.id} style={{borderTop:"1px solid #eee",padding:"11px 0"}}><strong>{s?.name||"Elev"} · {LABELS[r.status]||r.status}</strong><span style={{marginLeft:10,color:"#777"}}>{r.absence_date}</span>{r.note&&<div style={{color:"#687068",marginTop:4}}>Note: {r.note}</div>}</div>})}{!filtered.length&&<p style={{color:"#777"}}>Ingen fravær i den valgte periode.</p>}</section></section></main>;
}
