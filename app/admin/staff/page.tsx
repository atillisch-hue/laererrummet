"use client";

import Link from "next/link";
import {useEffect,useMemo,useState} from "react";
import {supabase} from "../../../lib/supabase";
import {hasRole} from "../../../lib/roles";

type Staff={user_id:string;display_name:string;role:string;active:boolean};
const roleLabels:Record<string,string>={teacher:"Lærer",admin:"Leder / admin",board:"Bestyrelse"};
const box={background:"white",border:"1px solid #dedbd2",borderRadius:14,padding:20};

export default function StaffAdminPage(){
 const[ready,setReady]=useState(false),[staff,setStaff]=useState<Staff[]>([]),[message,setMessage]=useState(""),[error,setError]=useState("");
 const[editing,setEditing]=useState<string|null>(null),[name,setName]=useState(""),[role,setRole]=useState("teacher"),[active,setActive]=useState(true),[search,setSearch]=useState("");
 async function load(){setError("");const{data,error}=await supabase.rpc("admin_staff_directory");if(error){setError(error.message);return}setStaff((data||[]) as Staff[])}
 useEffect(()=>{(async()=>{const{data}=await supabase.auth.getSession();const u=data.session?.user;if(!u||!hasRole(u,"admin")){window.location.replace("/");return}await load();setReady(true)})()},[]);
 function edit(s:Staff){setEditing(s.user_id);setName(s.display_name||"");setRole(s.role||"teacher");setActive(s.active!==false);setMessage("")}
 async function save(){if(!editing||!name.trim())return;setMessage("");const{error}=await supabase.rpc("update_staff_member",{p_user_id:editing,p_display_name:name.trim(),p_role:role,p_active:active});if(error){setMessage(`Kunne ikke gemme: ${error.message}`);return}setMessage("Medarbejderen er gemt. Ændringen slår automatisk igennem i mødebooking og ansvarslister.");setEditing(null);await load()}
 const shown=useMemo(()=>staff.filter(s=>(s.display_name||"").toLowerCase().includes(search.toLowerCase())),[staff,search]);
 if(!ready)return <main style={{padding:50}}>Henter personale…</main>;
 return <main style={{minHeight:"100vh",background:"#f5f2ea",color:"#26342e"}}><header style={{background:"#486b59",color:"white",padding:"20px 6vw"}}><div style={{maxWidth:1050,margin:"0 auto",display:"flex",justifyContent:"space-between",alignItems:"center",gap:16}}><div><small style={{fontWeight:800,letterSpacing:1.5}}>KLASSEVÆRELSET · ADMINISTRATION</small><h1 style={{fontFamily:"Georgia,serif",fontSize:38,margin:"5px 0 0"}}>Personale</h1></div><Link href="/admin" style={{color:"white",textDecoration:"none",border:"1px solid rgba(255,255,255,.5)",padding:"9px 13px",borderRadius:8}}>← Administration</Link></div></header><section style={{maxWidth:1050,margin:"0 auto",padding:"38px 24px"}}><p style={{fontSize:18,color:"#667068",maxWidth:760}}>Personale oprettes fortsat som brugere. Her styrer du deres navn, personalerolle og om de er aktive. Aktive medarbejdere bliver automatisk tilgængelige i møder, booking og handleplaner.</p>{error&&<div style={{...box,borderColor:"#c96b5c",color:"#7b2f25",margin:"20px 0"}}>Personale kunne ikke hentes: {error}</div>}{message&&<div style={{padding:14,background:"#e7eee9",borderRadius:9,fontWeight:700,margin:"20px 0"}}>{message}</div>}<div style={{display:"flex",justifyContent:"space-between",gap:12,alignItems:"center",flexWrap:"wrap",margin:"26px 0 14px"}}><h2 style={{fontFamily:"Georgia,serif",margin:0}}>Personalekatalog <span style={{fontFamily:"inherit",fontSize:18,color:"#788078"}}>({staff.filter(s=>s.active).length} aktive)</span></h2><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Søg personale…" style={{padding:"11px 13px",border:"1px solid #d8d4ca",borderRadius:8,minWidth:240}}/></div><div style={{display:"grid",gap:10}}>{shown.map(s=><article key={s.user_id} style={{...box,padding:16,opacity:s.active?1:.62}}><div style={{display:"flex",justifyContent:"space-between",gap:14,alignItems:"center",flexWrap:"wrap"}}><div><strong style={{fontSize:17}}>{s.display_name}</strong><div style={{color:"#727970",marginTop:3}}>{roleLabels[s.role]||s.role} · {s.active?"Aktiv":"Inaktiv"}</div></div><button onClick={()=>edit(s)} style={{padding:"8px 12px"}}>Redigér</button></div>{editing===s.user_id&&<div style={{marginTop:16,paddingTop:16,borderTop:"1px solid #eee",display:"grid",gridTemplateColumns:"minmax(220px,2fr) minmax(180px,1fr) auto",gap:10,alignItems:"center"}}><input value={name} onChange={e=>setName(e.target.value)} placeholder="Navn" style={{padding:11}}/><select value={role} onChange={e=>setRole(e.target.value)} style={{padding:11}}><option value="teacher">Lærer</option><option value="admin">Leder / admin</option><option value="board">Bestyrelse</option></select><label style={{display:"flex",gap:7,alignItems:"center"}}><input type="checkbox" checked={active} onChange={e=>setActive(e.target.checked)}/> Aktiv</label><div style={{gridColumn:"1/-1",display:"flex",gap:8}}><button onClick={save}>Gem</button><button onClick={()=>setEditing(null)}>Annuller</button></div></div>}</article>)}{!shown.length&&!error&&<div style={box}>Ingen medarbejdere matcher søgningen.</div>}</div><div style={{...box,marginTop:24,background:"#faf8f3"}}><strong>Ny medarbejder?</strong><p style={{marginBottom:0,color:"#687068"}}>Opret først medarbejderens konto under <Link href="/admin/users">Brugere & klasser</Link> og giv den rollen Lærer eller Admin. Derefter dukker medarbejderen op her.</p></div></section></main>
}
